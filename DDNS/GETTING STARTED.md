DDNS stand  for Dynamic Domain Name System, and it a service that automatically update the IP address associated with a domain name when the IP address changes.
Useful for devices that have dynamic IP addresses, which can change periodically.

